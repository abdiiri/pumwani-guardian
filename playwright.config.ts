import { createabdiiriConfig } from "abdiiri-agent-playwright-config/config";

export default createabdiiriConfig({
  // Add your custom playwright configuration overrides here
  // Example:
  // timeout: 60000,
  // use: {
  //   baseURL: 'http://localhost:3000',
  // },
});
